import rawCatalog from "../../../prisma/data/traditional-calendar-catalog.json";

export const TRADITIONAL_CALENDAR_VERSION = rawCatalog.version;
export const TRADITIONAL_CALENDAR_SCOPE = rawCatalog.scope;
export const TRADITIONAL_CALENDAR_SOURCES = rawCatalog.sources;
export const TRADITIONAL_ENTITIES = rawCatalog.entities;
export const TRADITIONAL_RELATIONS = rawCatalog.relations;
export const TRADITIONAL_METHOD_RULES = rawCatalog.methodRules;
export const TRADITIONAL_INTERPRETATIONS = rawCatalog.interpretations;
